 app.factory("AccountCategory", function($resource) {

  return $resource( api + "account_categories/:id", { id: '@id', search: '@search' }, {

    query: { method: 'GET', isArray: false },

    update: { method: 'PUT' },

    search: { method: 'GET' },

  });

});
